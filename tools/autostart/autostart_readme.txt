Global autostart files go in '/etc/xdg/autostart/' and you must 'chown root:root' them.
You must also 'chown root:root <keylogger_path>'.

Local (per-user) autostart entries go in '/home/<user>/.config/autostart/'.

All autostart filenames must end in ".desktop".
